# 实验应用执行约定

## 当前范围

项目是可通过指定远程HTTPS地址分享的实验应用。目标身份入口为邮箱、邮箱验证码和密码注册：验证码通过后创建账号并自动登录；此后仅用邮箱和密码登录，不再要求登录验证码。忘记密码时用邮箱验证码重置，已登录用户可校验当前密码后改密。邮箱验证码只证明邮箱控制权，不是年龄、用途或同意审批。用户不经过年龄选择、隐私确认、逐用途同意或拒绝/撤回界面、邀请码、验证短信或人工审批；真人告知审批、正式年龄核验、公众服务资格和保存期限审批不是STEP或阶段交接条件。

2026-09-23：S1-STEP03.5邮箱身份已实现，包括验证码注册、邮箱密码登录、找回和改密；新验证独立记录。真实SMTP未配置，验收使用隔离邮件替身。增量迁移保留历史账号，本机开发库旧用户名账号仅在独立预检后按授权清理。

本轮只实现STEP03身份、现有页面与品牌并更新相关文档，不实现聊天、记忆、画像、内部评测、优化或研究等后续STEP能力；不实际发布远程服务，不采购域名或服务器。后续功能按所属STEP实施并提供真实验收。

## 默认配置与用户历史分离

`experiment-defaults/1`将support、memory、profile、evaluation、optimization设为true；`implemented`单列已实现能力。此配置不是用户同意，不生成或批量补写granted。旧年龄、consent及其来源引用保留历史，未知年龄仍unknown，不默认成年；旧决定不作为当前入口或执行门槛。

新来源grant无需客户端consent_id；由服务端按业务动作建立owner/source/version/run关联，检查删除、版本和run状态。旧grant的consent引用保留，但不再参与新实验业务核权。来源隔离、版本冲突、删除传播、预算、取消及结果真实性始终保留。

## 记忆与重要操作

后续记忆提取完成后校验来源、schema、版本、任务代次和删除状态，业务事务自动保存。用户可查看、更正及确认删除；system_inference保留推断标识，不自动冒充用户确认事实。遗忘保留最小抑制标记，旧任务不得重新抽取已遗忘内容。

删除、外发和其他重要副作用继续确认。内部评测和优化保留账号与数据集隔离、来源追踪、固定评价标准及独立评价器，优化器不能修改holdout、预算或安全规则。未执行、失败、模拟与真实结果必须分开。

## 保存、配置与迁移

业务记录、来源关联、draft和实验产物不设固定TTL；登录8小时安全会话、短期邮箱验证码、运行deadline、租约和缓存回收是独立技术机制。既有expires_at历史值保留但不决定业务资料有效性。

运行时接受显式PostgreSQL地址及库名。自动化测试使用独立合成check/migration数据库，禁止连接开发库。远程访问仅配置单一HTTPS Origin，同域转发页面与/api/v1，保留Secure/HttpOnly/SameSite=Strict Cookie与精确Origin/CSRF校验，域名证书以后通过配置接入。

追加迁移e032_experiment_defaults仅使context_grants.consent_id可空，不清库、不改写历史用户决定。旧owner复合外键继续约束非空历史引用；存在新无consent关联时拒绝有损降级。历史迁移及执行收据不改写，新验证单独记录。启动、代理和检查见[DEVELOPMENT](DEVELOPMENT.md)。
